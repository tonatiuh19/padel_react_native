import { StyleSheet, Dimensions } from "react-native";

export const TimeSlotPickerStyles = StyleSheet.create({
  pickerContainer: {
    width: "100%",
    marginBottom: 10,
  },
  picker: {
    width: "100%",
  },
});
